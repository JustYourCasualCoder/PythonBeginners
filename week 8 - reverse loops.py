##start = int(input("input a number "))
##limit = int(input("input a limit "))
##
##while start >= limit:
##    print (start)
##    start -= 1
    
    
start = int(input("input a number "))
limit = int(input("input a limit "))

while start >= limit:
    if start % 2 == 0:
        print (start)
    start = start - 1
    
    
